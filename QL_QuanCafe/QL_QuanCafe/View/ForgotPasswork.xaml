<Window x:Class="QL_QuanCafe.View.ForgotPasswork"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QL_QuanCafe.View"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="ForgotPasswork" Height="400" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <materialDesign:Card
            Background="{DynamicResource PrimaryHueLightBrush}"
            Foreground="{DynamicResource PrimaryHueLightForegroundBrush}"
            HorizontalAlignment="Left"
            Grid.Row="0"
            Width="500"
            Padding="8">
            <TextBlock
                FontSize="16">
                Tìm tài khoản của bạn
            </TextBlock>
        </materialDesign:Card>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <materialDesign:Card
                Grid.Row="0"
                Width="500"
                Padding="8"
                UniformCornerRadius="6">
                <TextBlock
                    TextWrapping="Wrap"
                    FontSize="14">
                    Vui lòng nhập email để tìm kiếm tài khoản của bạn.
                 </TextBlock>    
            </materialDesign:Card>
            <TextBox
                materialDesign:HintAssist.Hint="Email"
                Grid.Row="1"
                x:Name="txt_Email"
                materialDesign:TransitionAssist.DisableTransitions="True" 
                Height="28"
                Width="400"/>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button x:Name="btn_Huy"
                Grid.Column="0"
                Style="{StaticResource MaterialDesignRaisedLightButton}"
                Width="100"
                ToolTip="Resource name: MaterialDesignRaisedLightButton" Click="btn_Huy_Click">
                    _HUY
            </Button>
            <Button
                x:Name="btn_TimKiem"
                Grid.Column="1"
                Style="{StaticResource MaterialDesignRaisedButton}"
                Width="100"
                ToolTip="Resource name: MaterialDesignRaisedButton" Click="btn_TimKiem_Click">
                    _TIM KIEM
            </Button>
        </Grid>
    </Grid>
</Window>
