<Page x:Class="QL_QuanCafe.View.PaymentView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QL_QuanCafe.View" xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PaymentView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <!--CAPTION-->
            <StackPanel Grid.Column="0"
                                    Orientation="Horizontal"
                                    VerticalAlignment="Center">
                <fa:IconImage Icon="MoneyBills"
                                          Height="20"
                                          Width="20"
                                           Foreground="{StaticResource primaryTextColor}"
                                          Margin="35,0,10,0"/>
                <TextBlock Text="Thanh toán"
                                       Foreground="{StaticResource primaryTextColor}"
                                       FontFamily="Montserrat"
                                       FontSize="16"
                                       FontWeight="Medium"
                                       VerticalAlignment="Center"/>

            </StackPanel>

            <!--StackPanel-->
            <StackPanel Grid.Column="1"
                                    Orientation="Horizontal"
                                    FlowDirection="RightToLeft"
                                    VerticalAlignment="Center"
                                    Margin="0,0,10,0">
                <Button Style="{StaticResource iconButton}"
                                    Tag="{StaticResource color1}">
                    <fa:IconImage Icon="AngleDown" Style="{StaticResource iconButtonIcon}"/>
                </Button>
                <TextBlock x:Name="tbUserName" Text="Hi Tuấn"
                                       Foreground="{StaticResource titleColor3}"
                                       FontFamily="Montserrat"
                                       FontSize="12"
                                       VerticalAlignment="Center"/>
                <Ellipse Height="35" Width="35"
                                     Stroke="{StaticResource color2}"
                                     StrokeThickness="2"
                                     Margin="10,0,10,0">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="/Image/user.png"/>
                    </Ellipse.Fill>
                </Ellipse>


            </StackPanel>
        </Grid>

        <Grid Grid.Row="1"
              Margin="10 20 10 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="10"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
               
                <Label Grid.Row="0" 
                       Content="ĐƠN HÀNG CHƯA THANH TOÁN"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       FontSize="16"/>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Column="0" Content="Số bàn" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <Label Grid.Column="1" Content="Tên khách hàng" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <Label Grid.Column="2" Content="Tổng tiền hóa đơn" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <Label Grid.Column="3" Content="Lựa chọn" FontWeight="Bold" Margin="25 0 0 0"/>
                    
                </Grid>
                
                <ScrollViewer Grid.Row="2" 
                              Margin="0 6">
                    <StackPanel x:Name="unPayedList">
                        <!--<PageContent x:Name="mainContent"/>-->
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <Grid Grid.Column="1" 
                  Background="{StaticResource color12}"
                  Margin="4 0"/>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                
                <Label Content="ĐƠN HÀNG ĐÃ THANH TOÁN"
                       Grid.Row="0"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"/>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Column="0" Content="Mã hóa đơn" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <Label Grid.Column="1" Content="Tên khách hàng" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <Label Grid.Column="2" Content="Tổng tiền hóa đơn" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <Label Grid.Column="3" Content="Chi tiết" FontWeight="Bold" Margin="25 0 0 0"/>

                </Grid>

                <ScrollViewer x:Name="payedList"
                              Grid.Row="2"
                              Margin="0 6">
                    <StackPanel x:Name="PayedList" >
                        <!--<PageContent x:Name="mainContent"/>-->
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Page>
